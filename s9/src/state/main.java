package state;

public class main {
    public static void main(String[] args) {
        ComandaMancare comanda = new ComandaMancare(10);
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();

    }
}
